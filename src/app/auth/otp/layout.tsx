import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Vérification OTP | Auchan Super Admin",
}

export default function OtpLayout({ children }: { children: React.ReactNode }) {
  return children
}
