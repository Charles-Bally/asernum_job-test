import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Nouveau mot de passe | Auchan Super Admin",
}

export default function NewPasswordLayout({ children }: { children: React.ReactNode }) {
  return children
}
