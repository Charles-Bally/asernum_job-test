import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Magasins | Auchan Super Admin",
}

export default function StoreLayout({ children }: { children: React.ReactNode }) {
  return children
}
