"use client";

import { Dialog } from "@/components/dialog_system/components/Dialog";

/**
 * Provider pour les dialogs
 * À placer dans le layout principal de l'application
 */
export function DialogProvider() {
  return <Dialog />;
}
